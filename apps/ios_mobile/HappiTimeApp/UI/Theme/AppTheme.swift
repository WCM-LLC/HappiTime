import SwiftUI

enum AppTheme {
    enum Colors {
        static let background = Color(red: 0.96, green: 0.94, blue: 0.92)
        static let surface = Color.white
        static let text = Color(red: 0.13, green: 0.13, blue: 0.14)
        static let mutedText = Color(red: 0.45, green: 0.46, blue: 0.49)
        static let primary = Color(red: 0.79, green: 0.59, blue: 0.35)
        static let border = Color.black.opacity(0.08)
        static let danger = Color.red
        static let success = Color.green
    }
}
