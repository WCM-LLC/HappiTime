// Types for bytesToHex.mjs (plain ESM impl; this declaration gives the app
// strict-mode types without compiling the runtime file).

export declare function bytesToHex(bytes: Uint8Array | number[]): string;
