# Testing

## Unit tests
```powershell
npm test
```

Current unit tests live in `test/` and run via Node’s built-in test runner.

## Lint + typecheck
```powershell
npm run lint
npm run typecheck
```

## Web build smoke
```powershell
npm run build:web
```

